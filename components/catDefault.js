import React from 'react'
import { View, Text } from 'react-native'

export default function CatDefault() {
    return (
        <View>
            <Text>esta es la default</Text>
        </View>
    )
}